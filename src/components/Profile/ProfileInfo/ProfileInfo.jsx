import React from "react";
import classes from "./ProfileInfo.module.css"

const ProfileInfo = () => {
    return <div className={classes.profileInfo}>Profile Info</div>
}

export default ProfileInfo;